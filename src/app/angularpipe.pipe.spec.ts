import { AngularpipePipe } from './angularpipe.pipe';

describe('AngularpipePipe', () => {
  it('create an instance', () => {
    const pipe = new AngularpipePipe();
    expect(pipe).toBeTruthy();
  });
});
