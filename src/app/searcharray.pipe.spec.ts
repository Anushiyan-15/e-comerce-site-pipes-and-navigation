import { SearcharrayPipe } from './searcharray.pipe';

describe('SearcharrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearcharrayPipe();
    expect(pipe).toBeTruthy();
  });
});
